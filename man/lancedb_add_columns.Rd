% Generated by roxygen2: do not edit by hand
\name{lancedb_add_columns}
\alias{lancedb_add_columns}
\title{Add Columns to a Table}
\usage{
lancedb_add_columns(table, transforms)
}
\arguments{
\item{table}{A \code{lancedb_table} object.}
\item{transforms}{A named list mapping new column names to SQL expressions.}
}
\value{
The table (invisibly).
}
\description{
Adds new columns computed from SQL expressions referencing existing columns.
}
\examples{
\dontrun{
# Add a column computed from existing ones
lancedb_add_columns(tbl, list(score_doubled = "score * 2"))

# Add a constant column
lancedb_add_columns(tbl, list(status = "'pending'"))
}
}
