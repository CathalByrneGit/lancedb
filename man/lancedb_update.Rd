% Generated by roxygen2: do not edit by hand
\name{lancedb_update}
\alias{lancedb_update}
\title{Update Rows in a Table}
\usage{
lancedb_update(table, values, where = NULL)
}
\arguments{
\item{table}{A \code{lancedb_table} object.}
\item{values}{A named list mapping column names to SQL expression strings.
For literal values, wrap strings in single quotes: \code{list(status = "'active'")}.
For computed values, use expressions: \code{list(score = "score + 1")}.}
\item{where}{Optional SQL WHERE predicate. If \code{NULL}, all rows are updated.}
}
\value{
The table (invisibly).
}
\description{
Updates column values for rows matching an optional filter predicate.
Values are specified as SQL expressions.
}
\examples{
\dontrun{
# Set all scores to 0
lancedb_update(tbl, values = list(score = "0"))

# Update matching rows
lancedb_update(tbl, values = list(score = "score * 2"), where = "category = 'A'")
}
}
