% Generated by roxygen2: do not edit by hand
\name{lancedb_alter_columns}
\alias{lancedb_alter_columns}
\title{Alter Columns in a Table}
\usage{
lancedb_alter_columns(table, alterations)
}
\arguments{
\item{table}{A \code{lancedb_table} object.}
\item{alterations}{A list of alteration specs. Each element is a named list
with \code{path} (required) and optional \code{rename} and/or \code{nullable}.}
}
\value{
The table (invisibly).
}
\description{
Modifies column properties such as name or nullability.
}
\examples{
\dontrun{
# Rename a column
lancedb_alter_columns(tbl, list(list(path = "old_name", rename = "new_name")))

# Make a column nullable
lancedb_alter_columns(tbl, list(list(path = "score", nullable = TRUE)))
}
}
