% Generated by roxygen2: do not edit by hand
\name{lancedb_fts_search}
\alias{lancedb_fts_search}
\title{Create a Full-Text Search Query}
\usage{
lancedb_fts_search(table, query_text, columns = NULL)
}
\arguments{
\item{table}{A \code{lancedb_table} object.}
\item{query_text}{Character string. The search query text. Can be a phrase
  (e.g., \code{"old man and the sea"}) or individual terms (e.g., \code{"old man sea"}).}
\item{columns}{Character vector. Optional column names to search in. If
  \code{NULL} (default), all FTS-indexed columns are searched.}
}
\value{
A \code{lancedb_lazy} object.
}
\description{
Begins a lazy full-text search (BM25-based) on a LanceDB table. Requires
an FTS index to have been created on one or more text columns. The query
is not executed until \code{\link{collect}} is called.
}
\examples{
\dontrun{
# Create FTS index first
lancedb_create_index(tbl, "content", index_type = "fts",
                     with_position = TRUE, stem = TRUE)

# Full-text search
results <- lancedb_fts_search(tbl, "machine learning") \%>\%
  select(title, content) \%>\%
  slice_head(n = 10) \%>\%
  collect()

# Search specific columns
results <- lancedb_fts_search(tbl, "neural networks",
                               columns = c("title", "abstract")) \%>\%
  collect()
}
}
\seealso{
\code{\link{lancedb_search}}, \code{\link{lancedb_create_index}}
}
