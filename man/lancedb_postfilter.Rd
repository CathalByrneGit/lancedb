% Generated by roxygen2: do not edit by hand
\name{lancedb_postfilter}
\alias{lancedb_postfilter}
\title{Enable Post-Filtering}
\usage{
lancedb_postfilter(.data)
}
\arguments{
\item{.data}{A \code{lancedb_lazy} object.}
}
\value{
A new \code{lancedb_lazy} object with post-filtering enabled.
}
\description{
When enabled, filter predicates are applied after the search rather than
before. This ensures the search always uses the full index but may return
fewer results than the requested limit after filtering.
}
\examples{
\dontrun{
# Post-filter: search first, then apply filter
lancedb_search(tbl, runif(128)) \%>\%
  filter(category == "science") \%>\%
  lancedb_postfilter() \%>\%
  slice_head(n = 10) \%>\%
  collect()
}
}
\seealso{
\code{\link{lancedb_search}}, \code{\link{lancedb_fast_search}}
}
